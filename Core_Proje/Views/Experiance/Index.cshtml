@using EntityLayer.Concrete
@model List<Experiance>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="card-header border-0">
    <h3 class="mb-0">Deneyim Listesi Tablosu</h3>
</div>
<!-- Light table -->
<div class="table-responsive">
    <table class="table align-items-center table-flush">
        <thead class="thead-light">
            <tr>
                <th scope="col" class="sort" data-sort="name">#</th>
                <th scope="col" class="sort" data-sort="budget">Başlık</th>
                <th scope="col" class="sort" data-sort="status">Tarih</th>
                <th scope="col">Sil</th>
                <th scope="col" class="sort" data-sort="completion">Düzenle</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody class="list">
            @foreach (var item in Model)
            {
                <tr>
                    <th>
                        @item.ExperianceID
                    </th>
                    <th class="budget">
                        @item.Name
                    </th>
                    <td>
                        @item.Date
                    </td>
                    <td><a href="/Experiance/DeleteExperiance/@item.ExperianceID" class="btn btn-danger">Sil</a></td>
                    <td><a href="/Experiance/EditExperiance/@item.ExperianceID" class="btn btn-info">Güncelle</a></td>
                </tr>
            }
        </tbody>
    </table>
    <a href="/Experiance/AddExperiance/" class="btn btn-primary">Yeni Deneyim Ekle</a>
</div>
